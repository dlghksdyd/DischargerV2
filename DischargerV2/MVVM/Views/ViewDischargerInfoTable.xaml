<UserControl x:Class="DischargerV2.MVVM.Views.ViewDischargerInfoTable"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:DischargerV2.MVVM.Views"
             xmlns:b="http://schemas.microsoft.com/xaml/behaviors"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="1000"
             DataContext="{StaticResource ViewModelDischarger}">
    <Grid Background="White">
        <StackPanel Orientation="Horizontal">
            <Label Content="Voltage"></Label>
            <Label x:Name="xVoltage" Content="{Binding Model.DischargerDatas[0].ReceiveBatteryVoltage}"></Label>
            <Label Content="Current"></Label>
            <Label x:Name="xCurrent" Content="{Binding Model.DischargerDatas[0].ReceiveDischargeCurrent}"></Label>
            <Label Content="Discharger State"></Label>
            <Label Content="{Binding Model.DischargerStates[0]}"></Label>
            <Label Content="Discharger Info"></Label>
            <Label x:Name="xDischargerName" Content="{Binding Model.DischargerInfos[0].Name}"></Label>
            <Button Width="100" Height="20" Content="방전 시작">
                <b:Interaction.Triggers>
                    <b:EventTrigger EventName="Click">
                        <b:InvokeCommandAction Command="{Binding StartDischargerCommand}">
                            <b:InvokeCommandAction.CommandParameter>
                                <MultiBinding Converter="{StaticResource StartDischargerCommandParamConverter}">
                                    <Binding ElementName="xDischargerName" Path="Content"></Binding>
                                    <Binding ElementName="xVoltage" Path="Content"></Binding>
                                    <Binding ElementName="xCurrent" Path="Content"></Binding>
                                </MultiBinding>
                            </b:InvokeCommandAction.CommandParameter>
                        </b:InvokeCommandAction>
                    </b:EventTrigger>
                </b:Interaction.Triggers>
            </Button>
        </StackPanel>
    </Grid>
</UserControl>
